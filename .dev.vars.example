# Copy to .dev.vars for local `wrangler dev` of the Worker. NEVER commit .dev.vars.
TELEGRAM_WEBHOOK_SECRET = "random-hex-string"  # openssl rand -hex 32
CRON_SECRET = "another-random-hex-string"      # openssl rand -hex 32
GITHUB_TOKEN = "ghp_..."                        # token with `repo` scope, to trigger the chat workflow
